# Ignore everything
*

# But not these files
!*.c
!*.h

# Also, don't ignore directories (so Git can traverse into them)
!*/

# Optional: keep the .gitignore itself
!.gitignore

# OS-specific
.DS_Store
Thumbs.db
